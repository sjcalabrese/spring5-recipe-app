package guru.springframework.spring5recipeapp.domain;

public enum Difficulty {

    //some think it's better practice to create a enum package to contain enums being used
    EASY, MODERATE, KIND_OF_HARD, HARD
}
